[package]
name = "siri_et_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.108"
derives = { path = "derives" }
parser = { path = "parser" }
